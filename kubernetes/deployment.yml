apiVersion: apps/v1
kind: Deployment
metadata:
  name: code
  labels:
    app: code
spec:
  replicas: 1
  selector:
    matchLabels:
      app: code
  template:
    metadata:
      labels:
        app: code
    spec:
      containers:
      - name: nginx
        image: cloud.canister.io:5000/miquelbar/code:{{BUILD_ID}}
        ports:
        - containerPort: 3000
          name: http
      imagePullSecrets:
      - name: regcred